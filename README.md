
  # Rental Property System

  This is a code bundle for Rental Property System. The original project is available at https://www.figma.com/design/RVAMSwig3KbHnKIpz92lcU/Rental-Property-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  